export declare function getBlacklistedSupply(index: number, decimals: number, blacklist: string[], calls: string[]): number;
export declare function getWhitelistedSupply(index: number, decimals: number, blacklist: string[], whitelist: string[], calls: string[]): number;
