import { type MerklChainId } from "@sdk";
export declare function getUserPositionsV2(positionsToCheck: any, // TODO: add typing (depends on campaign type)
chainId: MerklChainId, user: string): Promise<any>;
